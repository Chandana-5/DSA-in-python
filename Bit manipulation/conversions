#decimal to binary
n = int(input())
res = ""
while n > 0:
    res = str(n % 2) + res
    n = n // 2
ans=""
for i in range(len(res)-1,-1,-1):
    ans+=res[i]
print(ans)
#binary to decimal
s=input()
add=0
for i in range(len(s)-1,-1,-1):
    if s[i]=="1":
        add=add+2**i 
print(add)
        
